#to use streamlit
streamlit

#to deploy with streamlit
setuptools

# to use google's earth engine api
earthengine-api

#for map display
folium


# only import geemap, do not import ee. ee is default in geemap
# Install from git master: v0.37.1 has a bug where basemaps variable shadows the module import,
# causing "Box has no attribute xyz_to_folium" on Streamlit re-execution.
# Master fixed this with `from .basemaps import xyz_to_folium` instead of `from . import basemaps`.
geemap @ git+https://github.com/gee-community/geemap.git

# for authentication with google
google-auth

# For Gemini chatbot
google-generativeai

# Aider for genai import
google-genai

# Added Pillow (PIL) for image processing to Gemini 
Pillow


