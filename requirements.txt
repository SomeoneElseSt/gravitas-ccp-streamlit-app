#to use streamlit
streamlit

#to deploy with streamlit
setuptools

# to use google's earth engine api
earthengine-api

#for map display
folium


# only import geemap, do not import ee. ee is default in geemap
# geemap >= 0.27.0 uses xyzservices.TileProvider directly (no longer needs xyz_to_folium)
geemap>=0.27.0

# for authentication with google
google-auth

# For Gemini chatbot
google-generativeai

# Aider for genai import
google-genai

# Added Pillow (PIL) for image processing to Gemini 
Pillow

